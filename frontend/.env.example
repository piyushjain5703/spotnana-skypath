# ──────────────────────────────────────────────────────────
#  Frontend environment variables
#  Copy this file to .env.local for local development.
#  For Netlify, set these in Site settings > Environment variables.
# ──────────────────────────────────────────────────────────

# Backend API base URL (include protocol, no trailing slash).
# Leave empty when using the Vite dev proxy or Docker/Nginx reverse proxy.
# For Netlify deployment, set this to your deployed backend URL.
#   Example: https://skypath-api.example.com
VITE_API_URL=
